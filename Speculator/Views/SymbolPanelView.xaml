<UserControl x:Class="Speculator.Views.SymbolPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking" 
             xmlns:local="clr-namespace:Speculator.Views" 
             xmlns:ig="http://schemas.infragistics.com/xaml"
             xmlns:viewModels="clr-namespace:Speculator.ViewModels"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="600" 
             DataContext="{dxmvvm:ViewModelSource Type={x:Type viewModels:SymbolPanelViewModel}}">
    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding StartListenDataServiceCommand}"/>
    </dxmvvm:Interaction.Behaviors>

    <dxdo:DockLayoutManager>
        <dxdo:LayoutGroup>
            <dxdo:LayoutPanel Caption="Panel1">
                <ig:XamDataChart HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ig:XamDataChart.DataContext>
                        <viewModels:SymbolPanelViewModel/>
                    </ig:XamDataChart.DataContext>
                    <ig:XamDataChart.Axes>
                        <ig:CategoryDateTimeXAxis>
                            <ig:CategoryDateTimeXAxis.LabelSettings>
                                <ig:AxisLabelSettings Location="OutsideBottom" Extent="35"/>
                            </ig:CategoryDateTimeXAxis.LabelSettings>
                        </ig:CategoryDateTimeXAxis>
                        <ig:NumericYAxis>
                            <ig:NumericYAxis.LabelSettings>
                                <ig:AxisLabelSettings Location="OutsideLeft"/>
                            </ig:NumericYAxis.LabelSettings>
                        </ig:NumericYAxis>
                    </ig:XamDataChart.Axes>
                </ig:XamDataChart>
            </dxdo:LayoutPanel>
            <dxdo:LayoutPanel Caption="Panel3" ItemWidth="200">
                <Grid>
                    <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Aqua" 
                              FontSize="10" AutoGenerateColumns="None"
                              ItemsSource="{Binding Glass}">
                        <dxg:GridControl.View>
                            <dxg:TableView AutoWidth="True"></dxg:TableView>
                        </dxg:GridControl.View>
                        <dxg:GridControl.Columns>
                            <dxg:GridColumn Binding="{Binding Price}"></dxg:GridColumn>
                            <dxg:GridColumn Binding="{Binding Volume}"></dxg:GridColumn>
                        </dxg:GridControl.Columns>
                    </dxg:GridControl>
                </Grid>
            </dxdo:LayoutPanel>
        </dxdo:LayoutGroup>
    </dxdo:DockLayoutManager>

</UserControl>
